# Core dependencies for running and training
torch
transformers
datasets
numpy
datasets

# Optional, but highly recommended for fine-tuning, building the C++ kernel,
# and enabling all features.
pybind11
cmake
peft
accelerate
bitsandbytes

# Optional: lm-evaluation-harness for benchmark evaluation during training
# Install with: pip install lm-eval
lm-eval
